# Tarkov Helper 1.5.10 위치 좌표 재점검

## 확인된 문제

기존 API 갱신은 퀘스트 이름·목표·선행 조건만 갱신하고 지도 좌표는 이전 SQLite DB 값을 그대로 보존했습니다. 따라서 구형 DB에 잘못 들어 있던 좌표도 계속 유지됐습니다.

대표 사례인 등대 `Drug Trafficking`은 기존 좌표가 물 처리장 남쪽 방향에 표시됐으나, 실제 목표는 북쪽 기차역 컨테이너 구역입니다.

## 1.5.10 수정

- tarkov.dev API에서 최신 퀘스트 구조를 가져옵니다.
- TarkovTracker/tarkovdata의 퀘스트 GPS 백분율을 별도로 가져옵니다.
- 앱에 포함된 SVG 지도 크기와 `playerMarkerTransform` 역행렬로 GPS 백분율을 게임 좌표로 변환합니다.
- 퀘스트 BSG ID와 위치 목표 개수·순서가 안전하게 일치할 때만 새 좌표를 적용합니다.
- 일치가 불확실하면 임의로 덮어쓰지 않고 기존 좌표를 보존합니다.
- 좌표 서버가 일시적으로 실패해도 퀘스트 DB 갱신 자체는 계속 진행합니다.
- 번들 DB의 Drug Trafficking 두 목표 좌표도 북쪽 기차역 위치로 바로 수정했습니다.

## Drug Trafficking 검증값

- 지도 백분율: X 58.15%, Y 8.65%
- Lighthouse SVG: 3100 × 3700
- 지도 픽셀: X 1802.65, Y 320.05
- 앱 게임 좌표: X -252.65, Z -1731.45

API 갱신 완료 메시지에는 `최신 좌표`와 `기존 좌표 보존` 개수가 각각 표시됩니다.
