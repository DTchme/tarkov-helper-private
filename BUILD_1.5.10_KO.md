# Tarkov Helper 1.5.10 — 1.1 퀘스트 개편 대응 안정화판

## 핵심 변경

- 현재 선택한 프로필에 맞춰 `tarkov.dev`의 `regular` 또는 `pve` 퀘스트를 직접 불러옵니다.
- 퀘스트, 선행 조건, 목표, 제출 아이템, 선택형 퀘스트 테이블을 갱신합니다.
- 기존 아이템 DB에 BSG ID가 없어도 이름으로 매칭하고, 새 아이템은 최소 레코드를 만들어 제출 아이템이 누락되지 않게 합니다.
- 기존 지도 좌표는 **동일한 BSG 퀘스트 ID + 동일한 영어 목표 설명**이 남아 있을 때 보존합니다.
- DB 교체 전 `Assets/Backups`에 기존 `tarkov_data.db`를 백업합니다.
- 진행도 재구축은 로그 미리보기와 최종 확인이 끝난 뒤에만 진행도를 초기화합니다.
- 진행도 재구축 전 `Data/Backups`에 `user_data.db`를 백업하고, 실패 시 자동 복원을 시도합니다.
- 재구축 중에는 실시간 로그 감시를 잠시 중지하고 완료 후 다시 시작해 DB 쓰기 충돌을 막습니다.
- PvE, 일반 PvP, 시즌 PvP 로그를 구분하며 다른 프로필 로그는 제외합니다.
- 퀘스트 상태는 로그 문구가 아니라 `message.type` 10/11/12로 판정합니다.
- 바뀐 퀘스트 라인을 역추론하지 않고 로그에 실제 ID가 있는 퀘스트만 복원합니다.
- 완료, 실패뿐 아니라 마지막 이벤트가 시작인 퀘스트도 `Active`로 복원합니다.

## 빌드

1. Windows에서 이 ZIP을 완전히 압축 해제합니다.
2. `BUILD_NOW.cmd`를 실행합니다.
3. 완료되면 다음 파일이 생성됩니다.

```text
release\TarkovHelper_v1.5.10_1.1_windows_v3.zip
```

.NET 8 SDK가 없으면 빌드 스크립트가 폴더 내부 설치를 시도합니다. 이미 SDK가 설치되어 있으면 설치된 SDK를 사용합니다.

## 처음 사용할 때 권장 순서

1. 기존 프로그램 폴더와 `Data/user_data.db`를 별도로 복사해 둡니다.
2. 프로그램에서 **PvE 프로필**을 선택합니다.
3. 설정의 **현재 프로필 퀘스트 DB 갱신**을 실행합니다.
4. 갱신 완료 수치가 표시되는지 확인합니다.
5. Tarkov 로그 폴더를 지정합니다.
6. **퀘스트 진행도 재구축**을 실행합니다.
7. 미리보기에서 완료/실패/진행 중 퀘스트를 확인하고 적용합니다.

## 복원 가능한 범위

로그로 복원 가능:

- 시작한 퀘스트
- 완료한 퀘스트
- 실패한 퀘스트

로그만으로 복원 불가:

- 처치 수 7/15 같은 부분 진행도
- 아이템 제출 수 2/3
- 설치, 마킹, 방문 목표의 일부 완료 상태
- 이미 삭제된 과거 로그에만 있던 퀘스트

세부 목표 진행도는 0부터 다시 기록해야 합니다.

## 알려진 제한

- 앱은 퀘스트 데이터 DB 하나를 공용으로 사용합니다. PvE 데이터로 갱신한 뒤 일반 PvP를 볼 때는 프로필을 바꾸고 다시 DB 갱신해야 합니다.
- 시즌 PvP 진행도는 앱의 별도 프로필로 저장하지 않으며 일반 PvP 재구축 대상에서 제외합니다.
- API는 선행 퀘스트의 OR 그룹 번호를 제공하지 않습니다. 오래된 그룹을 보존하지 않고 API에 나온 선행 조건을 AND 관계로 저장하므로, 일부 특수 OR 선행 관계는 정확하지 않을 수 있습니다.
- 목표 설명이 바뀌면 해당 목표의 기존 지도 마커를 자동 연결할 수 없어 좌표가 사라질 수 있습니다.
- 동적 일일/주간 운영 퀘스트는 정적 퀘스트 DB로 추적하지 않습니다.
